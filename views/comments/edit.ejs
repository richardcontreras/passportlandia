<% include ../partials/header %> 

<h1 class='lead text-center'>Edit Comment</h1>

<form class='container form-group' action='/stamps/<%= stamp_id %>/comments/<%= comment._id %>?_method=PUT' method='POST'>
    <textarea class='form-control mb-1' type='text' name='comment[text]'><%= comment.text%></textarea>
            <div class='text-center'>
                <button class='btn btn-outline-info'>Edit</button>
            </div>
            <a class='ml-1' href='/stamps/<%= stamp_id %>'>Go Back</a>
        </form>

<% include ../partials/footer %> 